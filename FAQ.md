# ❓ ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ

## 🎯 ОБЩИЕ ВОПРОСЫ

### Как это работает на высоком уровне?

1. **Ты загружаешь файлы:**
   - Шаблон письма (DOCX) с переменными `{Имя}`, `{Email}` и т.д.
   - Таблицу контактов (CSV) с реальными данными

2. **Система делает:**
   - Читает переменные из шаблона
   - Читает столбцы из CSV
   - Предлагает автоматическое соответствие (маппинг)
   - Для каждого контакта генерирует отдельное письмо (DOCX + PDF)
   - Упаковывает всё в ZIP архив

3. **Ты скачиваешь:**
   - ZIP файл с готовыми письмами, готовыми к отправке

### Какой плюс использовать это вместо просто вставлять вручную?

- **Массовая генерация** - за минуту вместо часов ручной работы
- **Без ошибок** - переменные подставляются автоматически
- **Привет B2B sales!** - генерируй 1000 персонализированных предложений

### Можно ли использовать для отправки по email?

Текущая версия - только генерирует файлы. Но легко добавить интеграцию с SMTP (Gmail, Yandex и т.д.) для прямой отправки. Спрашивай - помогу добавить!

---

## 💻 ТЕХНИЧЕСКИЕ ВОПРОСЫ

### Почему Flask, а не Django/FastAPI?

- **Flask** - легче для такого небольшого приложения
- **Django** - избыточный для этой задачи (ORM, админпанель и т.д.)
- **FastAPI** - хорош, но Flask уже готов

Хочешь FastAPI? Переделаю за 30 минут!

### Почему не использовать Google Sheets API?

Мы используем CSV, а это:
- Работает offline
- Не требует аккаунта Google
- Проще для пользователя
- Быстрее

### Почему генерировать оба DOCX и PDF?

- **DOCX** - если нужны правки перед отправкой
- **PDF** - если нужно отправить в читаемом виде

Можно отключить PDF если не нужен (не генерирует зря).

---

## 🚀 РАЗВЕРТЫВАНИЕ

### Почему Render + Vercel, а не что-то другое?

| Вариант | Плюсы | Минусы |
|---------|-------|--------|
| Render + Vercel | Бесплатно, просто, быстро | Спящий режим на бесплатном tier'е Render |
| Railway | Без спящего режима | Нужно мониторить кредиты |
| AWS | Мощно, масштабируемо | Сложно и дорого для старта |
| PythonAnywhere | Просто для Python | Ограничения на веб-доступ (нужна платная версия для CORS) |
| Heroku | Был хороший | Закончился бесплатный tier в ноябре 2024 |

**Рекомендация:** Начни с Render + Vercel. Если будут проблемы - мигрируем на Railway за 10 минут.

### На сколько документов хватит бесплатного хостинга?

- **За раз:** Можешь генерировать 5000-10000 документов в одну сессию
- **В день:** Если пользователи активные - можешь обработать 50000+ документов
- **Лимиты:**
  - Render спит после 15 минут без запросов (просыпается за 30 сек)
  - Vercel нет лимитов на фронтенд

Если будет 100k+ документов в день - нужен платный tier ($8-15/месяц за Render).

### Как ограничить доступ к приложению?

Добавить простую авторизацию:
```python
# В app.py добавить:
@app.route('/login', methods=['POST'])
def login():
    token = request.json.get('password')
    if token == 'твой_пароль':
        return {'token': 'ok'}
    return {'error': 'Неправильный пароль'}, 401
```

Но базовая версия - без авторизации. Хочешь - добавим?

---

## 📊 РАБОТА С ДАННЫМИ

### Что если у меня не 100% CSV совпадает с шаблоном?

Система гибкая! Ты вручную маппишь переменные к столбцам. Допустим:

**Шаблон:**
```
{Получатель}
```

**CSV:**
```
Организация,Имя
Компания A,Иван
```

Ты выбираешь в маппинге: `{Получатель}` → `Имя`. Готово!

### Можно ли использовать формулы в CSV?

Нет - CSV это просто текст. Но можно:
1. Подготовить CSV в Excel
2. Сделать нужные вычисления
3. Экспортировать в CSV
4. Загрузить в систему

### Какие символы поддерживаются в переменных?

- **Поддерживаются:** буквы (A-Z, а-я), цифры (0-9), подчеркивание (_)
- **НЕ поддерживаются:** пробелы, спецсимволы, кириллица в имени переменной

**Правильно:** `{Организация}`, `{Email_основной}`, `{client_name}`  
**НЕПРАВИЛЬНО:** `{Организация 1}`, `{Email-основной}`, `{имя клиента}`

---

## 🐛 ОШИБКИ И РЕШЕНИЯ

### Ошибка: "CORS policy: No 'Access-Control-Allow-Origin'"

Значит, браузер не может достучаться до backend'а.

**Решения:**
1. Проверь URL в `index.html` (правильный ли API_BASE_URL?)
2. Убедись, что backend запущен
3. На Render перезагрузи сервис
4. Очисти кэш браузера (Ctrl+Shift+Delete)

### Ошибка: "ModuleNotFoundError: No module named 'docxtpl'"

На backend'е не установлены зависимости.

**Решение:**
```bash
pip install -r requirements.txt
```

Если на Render - перезагрузи (Settings → Manual Deploy).

### CSV не читается / кодировка ошибка

CSV может быть в разных кодировках (UTF-8, Windows-1252, и т.д.).

**Система пробует автоматически**, но если не помогает:

**Решение:**
1. Открой CSV в Excel
2. File → Save As
3. Выбери "CSV UTF-8"
4. Сохрани
5. Загрузи снова

### PDF не генерируется

Может быть, что на сервере нет LibreOffice (нужна для конвертации).

**Решение:**
1. Если нужны только DOCX - закомментируй `convert()` в app.py
2. На Render нужно добавить LibreOffice в build процесс

Если критично - скажи, добавлю.

### Переменные из шаблона не видны

Переменные нужно вставлять правильно в DOCX:

**Правильно:**
```
Уважаемый {Имя}!
```

**НЕПРАВИЛЬНО:**
```
Уважаемый { Имя }     ← пробелы!
Уважаемый [Имя]       ← квадратные скобки!
Уважаемый Имя         ← без скобок!
```

Используй **фигурные скобки** `{}` и **без пробелов**.

---

## 🎯 USE CASES И ПРИМЕРЫ

### Как это использовать в реальном бизнесе?

**Пример 1: Massmail B2B предложений**
- Шаблон: письмо-предложение
- CSV: 1000 компаний с контактами и суммами
- Результат: 1000 персонализированных предложений за 1 минуту

**Пример 2: Финансовые отчеты**
- Шаблон: письмо с таблицей финаналитики
- CSV: список клиентов и их КПИ
- Результат: индивидуальные отчеты для каждого

**Пример 3: Договоры**
- Шаблон: договор с пустыми полями
- CSV: данные клиентов, даты, суммы
- Результат: готовые к подписанию контракты

**Пример 4: Квитанции**
- Шаблон: квитанция об оплате
- CSV: номера заказов, суммы, клиенты
- Результат: 10000 квитанций за раз

### Я работаю в ком-панию. Это подойдет для отдела?

**Отлично подойдет для:**
- Sales - персонализированные предложения
- HR - письма сотрудникам, предложения от рекрутеров
- Finance - счета, отчеты, квитанции
- Legal - договоры, соглашения
- Marketing - информационные письма

**Не подойдет для:**
- Массовой email рассылки (используй Mailchimp)
- Динамических форм (используй Typeform)
- Баз данных (используй Airtable или 1С)

---

## 📈 РАСШИРЕНИЕ

### Как добавить асинхронную обработку для 10k+ документов?

Нужна очередь задач (Celery + Redis). Дам инструкцию, если понадобится.

### Как интегрировать с email?

Через SMTP можно отправлять документы прямо. Или интеграция с сервисом (Mailgun, SendGrid).

### Как добавить авторизацию?

Можно JWT токены, или просто простой пароль для защиты.

### Можно ли интегрировать с Zapier?

Да, можно создать Zapier action, который будет вызывать наш API.

### Как сделать мобильное приложение?

React Native или Flutter сможет вызывать тот же backend.

---

## 💡 ЛУЧШИЕ ПРАКТИКИ

### Как организовать CSV для лучшего результата?

```
Хорошее:
- Чистые данные (без пробелов в начале/конце)
- Правильные названия колонок
- Одна переменная = один столбец
- UTF-8 кодировка

Плохое:
- Смешанные форматы (Иван vs ivan)
- Пробелы в данных
- Слишком много колонок (1000+) - будет медленно
- Огромные значения в ячейках
```

### Как организовать шаблон для лучшего результата?

```
Хорошее:
- Четкие переменные {Переменная}
- Предпросмотр перед генерацией
- Простое форматирование

Плохое:
- Слишком сложное форматирование
- Слишком много картинок
- Переменные в заголовках/колонтитулах (иногда не работают)
```

### Как тестировать перед большой рассылкой?

1. Генерируй 5-10 документов (с тестовыми данными)
2. Проверь вручную
3. Если все OK - генерируй основную партию
4. Никогда не генерируй 100k документов без проверки 🎯

---

## 📞 КОГДА ПИСАТЬ РАЗРАБОТЧИКУ

**Пиши, если:**
- Что-то не работает
- Хочешь добавить функцию
- Нужна консультация по расширению
- Вопросы по продакшену

**Пример:**
- "Мне нужна асинхронная обработка для 50k документов в день"
- "Как добавить отправку по email?"
- "Нужна авторизация для защиты от спама"

---

**Готов помогать! 🚀**
