# Mail Generator - Генератор писем из шаблона

## 📋 Описание

Полный fullstack инструмент для массовой генерации писем (DOCX + PDF) из шаблона и таблицы контактов.

**Возможности:**
- ✅ Веб-интерфейс для загрузки файлов
- ✅ Автоматическое определение переменных в шаблоне
- ✅ Маппинг полей: свяжи переменные с колонками CSV
- ✅ Генерация DOCX и PDF для каждого контакта
- ✅ Загрузка архива со всеми документами
- ✅ Поддержка разных кодировок CSV
- ✅ Обработка ошибок и валидация

---

## 🚀 Быстрый старт

### Локальная установка

```bash
# 1. Клонируй репозиторий (или создай папку с файлами)
git clone <repo-url>
cd mail-generator

# 2. Установи зависимости
pip install flask flask-cors docxtpl pandas docx2pdf python-docx

# 3. Запусти backend
python app.py

# 4. Открой фронтенд
# Либо просто открой index.html в браузере
# Либо используй live server (в VS Code расширение Live Server)

# Backend будет доступен по http://localhost:5000
# Frontend по http://localhost:3000 (если используешь live server)
```

---

## 📁 Структура файлов

```
mail-generator/
├── app.py                 # Backend (Flask)
├── index.html            # Frontend (HTML + CSS + JS)
├── requirements.txt      # Зависимости Python
└── README.md            # Этот файл
```

---

## 🔧 Требования

### Backend (Python)
- Python 3.7+
- Flask 2.0+
- flask-cors
- pandas
- docxtpl
- docx2pdf
- python-docx

### Frontend
- Любой современный браузер (Chrome, Firefox, Safari, Edge)

---

## 📊 Формат CSV

CSV должен иметь разделитель **точка с запятой (;)** и кодировку **UTF-8**.

Обязательные столбцы:
- `Email` — для имени файла
- `Организация` — для имени файла (опционально)
- Прочие столбцы с данными (используются в шаблоне)

**Пример:**
```
Организация;Имя;Email;ИО
ООО Рога и Копыта;Иван;ivan@example.com;И.И.
АО Васяйка;Петр;petr@example.com;П.П.
```

---

## 📝 Формат DOCX шаблона

Используй переменные в формате `{имя_переменной}`:

```
Уважаемый {Имя}!

Спешу сообщить, что ваша организация "{Организация}" выбрана для участия.

С уважением,
{ИО}
```

Переменные можно вставлять в тексте, заголовках, таблицах и колонтитулах.

---

## 🌐 Развертывание на хостинге

### Рекомендуемые бесплатные хостинги

#### 1️⃣ **Render.com** (ЛУЧШИЙ ВАРИАНТ)
- ✅ Бесплатный tier достаточен для этого инструмента
- ✅ Автоматический деплой из GitHub
- ✅ Поддержка Python и Node.js
- ✅ CORS работает из коробки
- ❌ Приложение спит после 15 минут неактивности на бесплатном плане

**Инструкция:**

1. Создай репозиторий на GitHub с файлами:
   ```
   app.py
   index.html
   requirements.txt
   .gitignore
   ```

2. На Render.com создай:
   - **Backend Service** (Python 3.9)
     - Build command: `pip install -r requirements.txt`
     - Start command: `gunicorn app:app`
   - **Static Site** или просто захостируй index.html на отдельном сервисе

3. Обнови `API_BASE_URL` в index.html на URL backend'а Render

**requirements.txt:**
```
Flask==2.3.0
Flask-CORS==4.0.0
pandas==2.0.0
docxtpl==0.16.4
docx2pdf==1.4.3
python-docx==0.8.11
gunicorn==20.1.0
```

---

#### 2️⃣ **Vercel** (для фронтенда)
- ✅ Отличный хостинг для статического контента
- ✅ Быстрые глобальные CDN
- ✅ Бесплатный tier
- ❌ Нельзя работать с Python напрямую

**Инструкция:**
1. Выкладываешь только `index.html` на Vercel
2. Backend на Render'е (см. выше)
3. Обновляешь `API_BASE_URL` в index.html

---

#### 3️⃣ **Railway.app** (альтернатива Render)
- ✅ Хороший бесплатный tier ($5/месяц кредиты)
- ✅ Простой деплой
- ✅ Поддержка Python

**Инструкция:**
1. Создай repo на GitHub
2. Авторизуйся на railway.app через GitHub
3. Создай новый проект → выбери GitHub repo
4. Railway автоматически обнаружит Python
5. Добавь переменную `FLASK_ENV=production`

---

#### 4️⃣ **Heroku** (закончится бесплатная версия в 2025)
- ⚠️ Уже прекратили бесплатный tier
- Можно попробовать, если найдешь альтернативу

---

### Шаг за шагом: Развертывание на Render.com

**1. Подготовка**
```bash
# Создай папку проекта
mkdir mail-generator
cd mail-generator

# Инициализируй git
git init
echo ".env" > .gitignore
echo "__pycache__/" >> .gitignore
echo "*.pyc" >> .gitignore
```

**2. Создай requirements.txt**
```
Flask==2.3.0
Flask-CORS==4.0.0
pandas==2.0.0
docxtpl==0.16.4
docx2pdf==1.4.3
python-docx==0.8.11
gunicorn==20.1.0
```

**3. Обнови app.py**
```python
# В конце файла измени:
if __name__ == '__main__':
    port = os.environ.get('PORT', 5000)
    app.run(debug=False, host='0.0.0.0', port=int(port))
```

**4. Обнови index.html**
```javascript
// В начале script раздела:
// const API_BASE_URL = 'http://localhost:5000';  // для локальной разработки

// На продакшене:
const API_BASE_URL = 'https://твой-backend.onrender.com';
```

**5. Выложи на GitHub**
```bash
git add .
git commit -m "Initial commit: Mail Generator"
git push origin main
```

**6. На Render.com**
- New → Web Service
- Connect GitHub repo
- Branch: main
- Runtime: Python 3.9
- Build command: `pip install -r requirements.txt`
- Start command: `gunicorn app:app`
- Environment variables:
  ```
  FLASK_ENV=production
  ```

**7. Для фронтенда на Vercel**
- New Project → Import Git Repo (только с index.html)
- Готово!

---

## 🔒 Безопасность при развертывании

```python
# В app.py добавь для продакшена:
import os

if os.environ.get('FLASK_ENV') == 'production':
    app.config['DEBUG'] = False
    # Ограничь размер файла
    app.config['MAX_CONTENT_LENGTH'] = 10 * 1024 * 1024  # 10MB

# CORS только для разрешенных доменов
from flask_cors import CORS
CORS(app, resources={
    r"/api/*": {
        "origins": ["https://твой-фронтенд.com", "https://твой-фронтенд.vercel.app"]
    }
})
```

---

## 📱 Использование

### Локально
1. Запусти `python app.py`
2. Открой `index.html` в браузере (или live server)
3. Загрузи шаблон DOCX и CSV
4. Проверь маппинг полей
5. Кликни "Генерировать архив"
6. Архив с документами загрузится

### На хостинге
1. Просто открой фронтенд URL
2. Все остальное то же самое

---

## 🐛 Решение проблем

### CORS ошибка
```
Access to XMLHttpRequest has been blocked by CORS policy
```

**Решение:** Убедись, что `flask-cors` установлен и импортирован в app.py.

### PDF не генерируется
Может быть нужен `libreoffice`:
```bash
# Ubuntu/Debian
sudo apt-get install libreoffice

# macOS
brew install libreoffice

# Windows
# Скачай с https://www.libreoffice.org/
```

Или используй альтернативу в app.py (убери конвертирование в PDF):
```python
# Закомментируй эту часть:
# try:
#     convert(docx_path, pdf_path)
# except:
#     pass
```

### Файлы не загружаются
- Проверь размер файлов (max 50MB)
- Убедись, что CSV использует кодировку UTF-8
- Разделитель CSV должен быть `;` (точка с запятой)

### Переменные не обнаруживаются
- Используй формат `{имя_переменной}` в шаблоне (с фигурными скобками)
- Без пробелов: `{Имя}` ✓, `{ Имя }` ✗

---

## 📈 Масштабирование

Для больших объемов:
1. Добавь очередь задач (Celery + Redis)
2. Кэширование результатов
3. Асинхронная обработка
4. WebSocket для прогресса

Но для стартовых объемов текущая архитектура достаточна.

---

## 📄 Лицензия

MIT

---

**Вопросы?** Создай issue в репозитории!
